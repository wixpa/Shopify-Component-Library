MONGODB_URI=
PORT=4000

# Comma-separated browser origins allowed to call the API (CORS + credentials).
# Include your Vercel URL, e.g. https://shopify-component-library-alpha.vercel.app
FRONTEND_ORIGIN=https://your-app.vercel.app

# Optional: allow any https://*.vercel.app origin (previews). Use with care.
# ALLOW_VERCEL_APP_ORIGINS=true
