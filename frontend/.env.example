# Public Railway URL (no trailing slash). Set this in Vercel project env for production.
# Leave unset locally so /api is proxied to the backend by Vite.
# VITE_BACKEND_URL=https://your-service.up.railway.app
